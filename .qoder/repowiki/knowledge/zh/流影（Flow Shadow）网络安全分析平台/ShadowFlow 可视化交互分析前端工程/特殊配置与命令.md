开发环境要求 Node ≥ 16.13.1、Yarn ≥ 1.22.1；启动命令为 `yarn std start`（默认监听 localhost:8004），需先在 `packages/std/config-overrides.js` 中配置 devServer proxy 指向后端 server 地址；打包命令为 `yarn std build`，产物输出至 `packages/std/build`；提交必须使用 `yarn commit` 走 Angular 规范。