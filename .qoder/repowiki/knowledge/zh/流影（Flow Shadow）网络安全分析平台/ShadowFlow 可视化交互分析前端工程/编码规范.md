- ESLint 继承 airbnb + react-app + prettier/recommended，并关闭 prop-types、camelcase、no-console 等规则以保持风格一致
- 通过 Husky + lint-staged 在 pre-commit 阶段对 JS/TS 执行 eslint 与 prettier，对 CSS/LESS 执行 stylelint
- 使用 Commitizen + cz-conventional-changelog 配合 @commitlint/config-conventional 强制 Angular 风格的提交信息
- 子包通过 yarn workspaces 暴露为独立 npm 包名（@shadowflow/components、@shadowflow/std），由根 package.json 统一管理 scripts 与工作区