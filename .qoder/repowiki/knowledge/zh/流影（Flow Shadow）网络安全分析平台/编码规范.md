- 跨子模块通信协议统一使用 protobuf 定义在 src/common 下（event.proto、feature.proto、evidence.proto、policy.proto、config.proto 等），由 ly_analyser 与 ly_server 共同引用。
- 每个子工程均自包含依赖包（dependence/）与源码（src/），并通过各自 Makefile 独立编译，避免交叉依赖。