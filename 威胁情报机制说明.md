# 流影威胁情报（threatinfo / threatinfopro）机制说明

> 结论整理日期：2026-08-13
> 来源：threatinfo.cpp / threatinfopro.cpp / tic.h / tic.cpp / 前端 ly_vis 源码排查

## 一、核心结论

1. **有对应的远程 API**：威胁情报是流影官方**远程商业服务**（受 APISIX 网关 + JWT / HTTPS 时间戳令牌双重认证保护），并非本地内置数据。
2. **前端没有 key 判断逻辑**：前端只是把 IP/域名透传给后端 CGI，对"无情报"结果静默降级（隐藏威胁标签，不报错）。
3. **判断逻辑在后端 CGI**：threatinfo/threatinfopro 读取本地配置文件，未配置时返回 `403 key invalid` / `403 token invalid`。
4. **当前服务器状态**：威胁情报服务**未配置**（`/Server/etc/tic.conf` 不存在，`/Server/etc/tisrs.conf` 的 KEY/HOST/PORT 为空），因此接口返回 key invalid / token invalid，属正常未启用状态，**不影响其他任何功能**。

## 二、后端 CGI 逻辑（判断所在）

### 1. threatinfo（单点情报查询）
- 读取 `/Server/etc/tisrs.conf`（define.h 中 `TISRS_CONF`）的 `KEY / HOST / PORT`
- `KEY` 为空 → 返回 403 `key invalid`
- 有 KEY 时流程：
  1. `POST http://HOST:PORT/apisix/plugin/jwt/sign?key=KEY` 换取 **JWT**
  2. 携带 JWT 访问远程威胁情报 API（**APISIX 网关认证**）
- 超时 5 秒

### 2. threatinfopro（批量情报查询）
- 读取 `/Server/etc/tic.conf`（define.h 中 `TIC_CONF`）的 `API_KEY / HOST / PORT`
- `API_KEY` 为空 → 返回 403 `token invalid`
- 有 KEY 时流程：
  1. `tic::generate_token_from_key(key)`：基于 **API_KEY + 授权起始时间（AUTH_START_TIME）+ 有效时长（AUTH_VALID_TIME）** 生成时间戳令牌（商业授权有效期机制）
  2. `POST https://HOST:PORT`（**HTTPS**）透传查询参数，附加 `token`
  3. 远程返回 200 → 透传结果给前端

## 三、前端逻辑（无 key 输入）

- `packages/std/src/service/api/util-api.js`：
  ```js
  export function threatinfo(key, op = 'get') {   // key 是 IP/域名，不是 API key！
      return fetch.post('threatinfo', { key, op })
  }
  export function threatinfoPro(list, type) {
      return fetch.post('threatinfopro', { list, type })
  }
  ```
- `packages/std/src/service/fetch.js`：threatinfo 接口设 **30 秒超时**（远程 API 可能较慢）
- `packages/std/src/utils/methods-data.jsx`：调用 `threatinfo(ip)` 获取威胁标签/参与风险评分；返回结果 `length <= 1`（空）时 `resolve(false)` → **静默隐藏威胁标签，页面正常不报错**
- `packages/std/src/page/event/page-child/event-detail/components/view-report/index.jsx`：事件详情报告中展示威胁情报

## 四、相关代码/配置文件

| 项 | 路径 |
|---|---|
| threatinfo CGI 源码 | `ly_server_src/server/threatinfo.cpp` |
| threatinfopro CGI 源码 | `ly_server_src/server/threatinfopro.cpp` |
| token 生成库 | `ly_analyser_src/common/tic.h` / `tic.cpp`（`generate_token_from_key`） |
| 配置 1（threatinfopro 用） | `/Server/etc/tic.conf`（`API_KEY/HOST/PORT`）—— **当前不存在** |
| 配置 2（threatinfo 用） | `/Server/etc/tisrs.conf`（`KEY/HOST/PORT`）—— 存在但全部为空 |
| 路径宏 | `define.h`：`TIC_CONF`、`TISRS_CONF` |
| 前端 API | `ly_vis/packages/std/src/service/api/util-api.js` |

## 五、启用步骤（需商业授权）

1. 向流影官方申请威胁情报服务授权（获取 key 与远程服务地址）
2. 创建 `/Server/etc/tic.conf`：
   ```
   API_KEY=<申请的key>
   HOST=<远程服务地址>
   PORT=<远程服务端口>
   ```
3. 编辑 `/Server/etc/tisrs.conf`：
   ```
   KEY=<申请的key>
   HOST=<远程服务地址>
   PORT=<远程服务端口>
   ```
4. 无需重启 Apache（CGI 每次请求读取配置），浏览器刷新页面即可生效

## 六、当前接口验证记录

```
threatinfo:     key invalid        （tisrs.conf KEY 为空）
threatinfopro:  token invalid      （tic.conf 不存在，API_KEY 为空）
```

两者均属"未配置"正常返回，前端自动降级，页面功能不受影响。
